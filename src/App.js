import React from "react";
import StartApp from "@components/StartApp";
import { ScrollView } from "react-native";
const App = () => {
  return (
    <>
      <ScrollView>
        <StartApp />
      </ScrollView>
    </>
  );
};

export default App;
